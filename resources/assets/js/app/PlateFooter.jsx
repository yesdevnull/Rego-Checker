define(['react'], function (React) {
    'use strict';

    var PlateFooter = React.createClass({
        render: function () {
            return (
                <footer>
                    Footer!
                </footer>
            );
        }
    });

    return PlateFooter;
});